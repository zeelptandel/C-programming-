#include <stdio.h>

int main() {
    int n, i, sum = 0;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    
    for (i = 1; i <= n; i++) {
        sum += 2 * i - 1;  // Adding the i-th odd number
    }
    
    printf("Sum of the first %d odd numbers is: %d
", n, sum);
    return 0;
}
