#include <stdio.h>

int main() 
{
    for (int n = 2; n <= 500; n++)
{
        int is_prime = 1;
        for (int i = 2; i <= n / 2; i++)
            if (n % i == 0)
                is_prime = 0;
        if (is_prime)
            printf("%d ", n);
    }
    return 0;
}
