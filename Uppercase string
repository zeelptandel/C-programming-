#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main() {
    char str[] = "Hello World 123!";

    printf("Original string: %s\n", str);

    for (int i = 0; i < strlen(str); i++) {
        str[i] = toupper(str[i]);
    }

    printf("Uppercase string: %s\n", str);

    return 0;
}
