#include <stdio.h>

int main() {
    int n, i;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    printf("The first %d natural numbers are:
", n);
    for (i = 1; i <= n; i++) {
        printf("%d ", i);
    }
    printf("
");
    return 0;
}
